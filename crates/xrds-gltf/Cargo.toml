[package]
name = "xrds-gltf"
version = "0.1.0"
edition = "2021"
description = "GLB/glTF 2.0 export from XrdsSceneDocument — no Bevy dependency"

[dependencies]
xrds-scene-graph = { workspace = true }
gltf-json = { version = "1.4", features = ["names", "extras"] }
serde_json = "1"
